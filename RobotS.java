//While we don't have another robot design just use the design of the RobotB
public class RobotS extends RobotB
{   
    public RobotS(String robotName, Pose pose)
    {
        //call the constructor of the superclass(RobotB)
        super(robotName, pose);
    }
    
}
